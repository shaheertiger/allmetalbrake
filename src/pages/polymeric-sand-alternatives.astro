---
import BaseLayout from '../layouts/BaseLayout.astro';
import body from '../bodies/polymeric-sand-alternatives.html?raw';

const title = "Polymeric Sand Alternatives — What Else Actually Works in Paver Joints | Home Fix Reviews";
const description = "Polymeric sand has a real failure rate and a set of conditions it will not tolerate. The six alternatives worth considering — PermaSand, EnviroSAND, wide-joint sand, plain jointing sand, open-graded chip — and which problem each one solves.";
---

<BaseLayout title={title} description={description} articleCss={true} articleBody={body}>
  <Fragment set:html={body} />
</BaseLayout>
