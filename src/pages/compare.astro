---
import BaseLayout from '../layouts/BaseLayout.astro';
import body from '../bodies/compare.html?raw';

const title = "Compare Home Improvement Products — Home Fix Reviews";
const description = "Compare driveway sealers, deck stains, garage floor coatings, and more side by side. Filter by type, coverage, and budget to find your match.";
---

<BaseLayout title={title} description={description} articleCss={false} articleBody={body}>
  <Fragment set:html={body} />
</BaseLayout>
